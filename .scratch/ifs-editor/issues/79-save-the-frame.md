# Saving the frame as a PNG

Status: resolved

Blocked by: 21.

There was no way to get a picture of a frame out of the editor.

## Acceptance

- The frame is written at the stage size, opaque, as the viewport shows it,
  and the viewport is left as it was. Tested with the host against
  `title.ifs`.
- Without a preview host it says why instead of writing a file. Tested under
  `ci`.
