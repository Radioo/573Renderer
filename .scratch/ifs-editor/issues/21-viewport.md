# Viewport

Status: resolved

Blocked by: 19.

## Acceptance

- The viewport widget shows the texture the host renders into, and asks the host to resize when the panel size changes.
- Picking an animation in the tree selects it in the host and shows the frame the timeline is on.
- A frame the host could not render leaves the last good frame on screen and shows the error once.
