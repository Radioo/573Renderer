# Documentation

Status: resolved

`docs/editor.md` rewritten per area as each lands; `CONTEXT.md` gains selection bar, command and notice.

Landed: docs/editor.md was rewritten area by area as each issue landed (the
command registry, the shell, command search, notices, the selection bar,
popovers, the inspector's Content and Keyframes sections, the timeline's lane,
column headers and zoom, the stage bar and tools, the graph, clip navigation
and editing in place, the package tabs, the library, projects and the start
screen), and CONTEXT.md gained an "The editor's own parts" section for command,
refusal, selection bar, notice and in place.
