# Window shell and layout

Status: resolved
Blocked by: 01

The top bar, the new default dock layout and the status bar sections.

## Acceptance

- Top bar: undo, redo, history, command search field, file name and unsaved count, project chip, Export to IFS with the count of changed owned depths and project images, Save.
- Default layout: package and library left, stage centre, timeline under both, inspector with History as a tab on the right at full height; the docks layout version is raised.
- Status bar sections: preview host state and render time, stage size and rate, frame, what is chosen, snapping, stage zoom.
