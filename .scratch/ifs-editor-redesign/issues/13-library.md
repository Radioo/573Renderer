# Library tiles

Status: resolved

Thumbnail tiles with use counts and type filters; drop on the inspector Character row; Enter places on the first free depth; New empty sprite without a prompt.

Landed: thumbnails on every character that draws an image, the four kind
filters stacked with the text filter, Return to place on the first free depth,
a drop target on the inspector's Character row, and New empty sprite without a
prompt. The rows stay a tree so the drag, the menus and the use counts keep
working. Covered by a window test. Documented in docs/editor.md.
